const {sequelize, DataTypes, Model} = require('../../db');


const { Dog } = require('../dog')
const { Dogs } = require('../dogs');

Dog.belongsTo(Dogs);
Dogs.hasMany(Dog);

module.exports = {Dogs, Dog, sequelize};